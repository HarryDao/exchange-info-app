export interface ScrollPosition {
  x: number;
  y: number;
}

export interface TouchPosition {
  x: number | null;
  y: number | null;
}

export interface DimensionSizes {
  width: number;
  height: number;
}
